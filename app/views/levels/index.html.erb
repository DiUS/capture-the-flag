<h1>Levels
<%= link_to new_level_path, class: "btn btn-lg btn-success pull-right" do %>
  <i class="fa fa-plus"></i> Add Level
<% end %>
</h1>
<p class="lead">Here you will find various capture the flag levels that other users have uploaded. Find one that looks interesting and test your skill!</p>

<% if !@levels.empty? %>
  <div class="levels-table">
    <table class="table table-striped table-bordered">
    	<tr>
    		<th>Name</th>
    		<th>Description</th>
    		<th></th>
    	</tr>
      <%- for level in @levels %>
        <tr>
        	<td><%= level.name %></td>
        	<td><%= level.description %></td>
        	<td>
            <%= link_to new_game_path(level_id: level), class: 'btn btn-lg btn-primary' do %>
              <i class="fa fa-plus"></i> Create Game
            <% end %>
          </td>
        </tr>
      <%- end %>
    </table>
  </div>
<% end %>
